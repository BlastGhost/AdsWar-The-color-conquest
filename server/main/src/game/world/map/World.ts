import { GPS } from "../../data/WorldGPS.js";



export default class World {
    public readonly height = GPS.AMPLITUDE_LATITUDE;
    public readonly width = GPS.AMPLITUDE_LONGITUDE;

    

    
    
    
    constructor() {
        
    }
}